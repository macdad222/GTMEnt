# Enterprise Strategy Platform - Environment Variables
# Copy this file to .env and fill in values
#
# NOTE: Most configuration is now managed via the Admin UI at /settings
# These environment variables are optional overrides for advanced deployments.

# =============================================================================
# APP SETTINGS
# =============================================================================

# Environment mode (development, production)
APP_ENV=production

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# DATA STORAGE
# =============================================================================

# ChromaDB vector store path (for embeddings)
CHROMA_PERSIST_DIR=/app/data/chroma

# =============================================================================
# LLM API KEYS (Optional - can also be set in Admin UI)
# =============================================================================
# These are read on startup but can be overridden via Admin UI

# xAI Grok (Recommended for LLM analysis and Voice AI)
# XAI_API_KEY=xai-...

# OpenAI (Alternative LLM provider)
# OPENAI_API_KEY=sk-...

# Anthropic Claude (Alternative LLM provider)
# ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# ADVANCED SETTINGS (Usually not needed)
# =============================================================================

# Database URL (future: PostgreSQL support)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/gtm_ent

# SEC EDGAR Comcast CIK (for SEC filings ingestion)
# SEC_EDGAR_COMCAST_CIK=0001166691

# Comcast Business website (for product page scraping)
# COMCAST_BUSINESS_BASE_URL=https://business.comcast.com
